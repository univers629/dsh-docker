diff --git a/packages/client/connection/src/client/index.ts b/packages/client/connection/src/client/index.ts
index 9847d48..7c61d35 100644
--- a/packages/client/connection/src/client/index.ts
+++ b/packages/client/connection/src/client/index.ts
@@ -54,0 +55,6 @@ export const inject: string[] = []
+/** Whether the authenticated reverse proxy opted this browser into Host settings. */
+function hasPublicLocalModeCookie(): boolean {
+  if (typeof document === 'undefined') return false
+  return document.cookie.split(';').some((entry) => entry.trim() === 'DSH_PUBLIC_LOCAL_MODE=1')
+}
+
@@ -106 +112,3 @@ export function apply(ctx: Context): void {
-    isLoopback: pageLocation === undefined || isLoopbackHostname(pageLocation.hostname),
+    isLoopback: pageLocation === undefined
+      || isLoopbackHostname(pageLocation.hostname)
+      || hasPublicLocalModeCookie(),
diff --git a/packages/client/connection/tests/client-apply.client.spec.ts b/packages/client/connection/tests/client-apply.client.spec.ts
index b7f6ebe..75553bf 100644
--- a/packages/client/connection/tests/client-apply.client.spec.ts
+++ b/packages/client/connection/tests/client-apply.client.spec.ts
@@ -14,0 +15 @@ type WebSocketGlobal = { WebSocket?: typeof WebSocket }
+type DocumentGlobal = { document?: { cookie: string } }
@@ -54,0 +56 @@ afterEach(() => {
+  delete (globalThis as DocumentGlobal).document
@@ -86,0 +89,10 @@ describe('connection client apply', () => {
+  it('treats the authenticated public-local marker as a loopback browser', async () => {
+    ;(globalThis as Win).location = { hostname: 'agent.example', search: '' }
+    Object.defineProperty(globalThis, 'document', {
+      configurable: true,
+      value: { cookie: 'DSH_PUBLIC_LOCAL_MODE=1' },
+    })
+
+    expect((await mount()).isLoopback).toBe(true)
+  })
+
